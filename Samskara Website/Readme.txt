How to start the project:

-Open a terminal
-cd in the website folder


- run pip install -r requirements.txt to install packages and dependencies
- run " set FLASK_APP=app " to tell flask where the app is located
- run python manage.py refresh_db 
- run flask run and go to http://127.0.0.1:5000 to open the app
